# ----- INPUTS ----- #

input LoginUserInput {
  email: String!
  password: String!
}

# ----- MUTATION ----- #

extend type Mutation {
  loginUser(data: LoginUserInput): User!
}

# ----- QUERY ----- #

extend type Query {
  isAuthenticated: Boolean
  logoutUser: Boolean
}

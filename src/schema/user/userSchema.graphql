# ----- INPUTS ----- #

input LoginUserInput {
  email: String!
  password: String!
}

# ----- MUTATION ----- #

extend type Mutation {
  loginUser(data: LoginUserInput): User!
}

# ----- QUERY ----- #

extend type Query {
  currentUser: User!
  isAuthenticated: Boolean
  logoutUser: Boolean
}

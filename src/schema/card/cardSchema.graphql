# ----- INPUTS ----- #

input YoutubeCardInput {
  videoId: String!
}

# ----- MUTATION ----- #

extend type Mutation {
  createYoutubeCard(data: YoutubeCardInput!): Card!
}

# ----- QUERY ----- #

extend type Query {
  userCards: [Card!]!
}
